Handlebars.registerHelper('wea', function() {
    return "ki onda"
    // text = Handlebars.Utils.escapeExpression(text);
    // text = text.replace(/(\r\n|\n|\r)/gm, '<br>');
    // return new Handlebars.SafeString(text);
});